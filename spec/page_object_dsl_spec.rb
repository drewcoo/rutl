require "spec_helper"

describe PageObjectDsl do
  it "has a version number" do
    expect(PageObjectDsl::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
